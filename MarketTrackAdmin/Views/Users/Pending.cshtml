@model IEnumerable<Appwrite.Models.Document>
@{
    ViewData["Title"] = "Pending Users";
}

<div class="container-fluid mt-4">
    <div class="row mb-4">
        <div class="col-12">
            <h2>👥 অনুমোদনের অপেক্ষায়</h2>
            <p class="text-muted">নতুন রেজিস্ট্রেশনের অনুমোদন/প্রত্যাখ্যান করুন</p>
        </div>
    </div>

    @if (Model == null || !Model.Any())
    {
        <div class="card p-5 text-center">
            <h4>✅ কোন অপেক্ষমাণ ইউজার নেই</h4>
            <p class="text-muted">সব ইউজার অনুমোদিত</p>
        </div>
    }
    else
    {
        <div class="row">
            @foreach (var user in Model)
            {
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="card p-4">
                        <div class="d-flex justify-content-between align-items-start mb-3">
                            <div>
                                <h5 class="mb-1">@user.Data["name"]</h5>
                                <small class="text-muted">@user.Data["email"]</small>
                            </div>
                            <span class="badge bg-warning">⏳ Pending</span>
                        </div>

                        <div class="mb-3">
                            <p class="mb-1"><strong>📱 Phone:</strong> @user.Data["phone"]</p>
                            <p class="mb-1"><strong>📍 Address:</strong> @user.Data["address"]</p>
                            <p class="mb-1"><strong>📅 Registered:</strong> @user.CreatedAt</p>
                        </div>

                        <div class="d-grid gap-2">
                            <form method="post" asp-action="Approve" asp-controller="Users">
                                <input type="hidden" name="userId" value="@user.Id" />
                                <input type="hidden" name="userName" value="@user.Data["name"]" />
                                <button type="submit" class="btn btn-success w-100">
                                    ✅ অনুমোদন করুন
                                </button>
                            </form>
                            <button type="button" class="btn btn-danger" 
                                    data-bs-toggle="modal" 
                                    data-bs-target="#rejectModal@(user.Id)">
                                ❌ প্রত্যাখ্যান করুন
                            </button>
                        </div>

                        <!-- Reject Modal -->
                        <div class="modal fade" id="rejectModal@(user.Id)" tabindex="-1">
                            <div class="modal-dialog">
                                <div class="modal-content bg-dark text-white">
                                    <div class="modal-header">
                                        <h5 class="modal-title">প্রত্যাখ্যানের কারণ</h5>
                                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                                    </div>
                                    <form method="post" asp-action="Reject" asp-controller="Users">
                                        <div class="modal-body">
                                            <input type="hidden" name="userId" value="@user.Id" />
                                            <input type="hidden" name="userName" value="@user.Data["name"]" />
                                            <div class="mb-3">
                                                <label class="form-label">কারণ লিখুন:</label>
                                                <textarea name="reason" class="form-control" rows="3" required></textarea>
                                            </div>
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">বাতিল</button>
                                            <button type="submit" class="btn btn-danger">প্রত্যাখ্যান নিশ্চিত করুন</button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>
