@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="bn">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MarketTrack Admin - লগইন</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/dark-theme.css">
    <style>
        body {
            background: linear-gradient(135deg, #0B1020 0%, #121A33 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .login-card {
            background: #1a2332;
            border-radius: 16px;
            padding: 40px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.3);
            max-width: 450px;
            width: 100%;
        }
        .logo {
            text-align: center;
            margin-bottom: 30px;
        }
        .logo-icon {
            background: linear-gradient(135deg, #2E5BFF 0%, #00C2A8 100%);
            width: 80px;
            height: 80px;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
            margin-bottom: 15px;
        }
        .form-control {
            background: #0B1020;
            border: 1px solid #2a3750;
            color: #fff;
            padding: 12px 16px;
            border-radius: 8px;
        }
        .form-control:focus {
            background: #121A33;
            border-color: #2E5BFF;
            color: #fff;
            box-shadow: 0 0 0 0.2rem rgba(46, 91, 255, 0.25);
        }
        .btn-login {
            background: linear-gradient(135deg, #2E5BFF 0%, #00C2A8 100%);
            border: none;
            padding: 12px;
            border-radius: 8px;
            font-weight: 600;
            font-size: 16px;
        }
        .btn-login:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(46, 91, 255, 0.4);
        }
        .alert {
            border-radius: 8px;
        }
    </style>
</head>
<body>
    <div class="login-card">
        <div class="logo">
            <div class="logo-icon">📍</div>
            <h2 class="text-white mb-2">MarketTrack Admin</h2>
            <p class="text-muted">অ্যাডমিন প্যানেলে স্বাগতম</p>
        </div>

        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger" role="alert">
                ❌ @ViewBag.Error
            </div>
        }

        <form method="post" asp-action="Login">
            <div class="mb-3">
                <label class="form-label text-white">ইমেইল</label>
                <input type="email" name="email" class="form-control" 
                       placeholder="your@email.com" required 
                       value="kawsarHossain31de@gmail.com">
            </div>

            <div class="mb-4">
                <label class="form-label text-white">পাসওয়ার্ড</label>
                <input type="password" name="password" class="form-control" 
                       placeholder="••••••••" required
                       value="12345678">
            </div>

            <div class="mb-3 form-check">
                <input type="checkbox" class="form-check-input" id="remember">
                <label class="form-check-label text-white" for="remember">
                    মনে রাখুন
                </label>
            </div>

            <button type="submit" class="btn btn-login text-white w-100">
                🔐 লগইন করুন
            </button>
        </form>

        <div class="mt-4 text-center">
            <small class="text-muted">
                ডিফল্ট অ্যাডমিন: kawsarHossain31de@gmail.com / 12345678
            </small>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
